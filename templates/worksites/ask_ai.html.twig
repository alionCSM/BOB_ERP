{#
    ask_ai endpoint is a JSON API (no HTML view rendered).
    This template is a placeholder in case a standalone UI page is needed.
    The actual AI endpoint logic lives in WorksitesController::askAi()
    and returns JSON directly — it does not render this template.
#}
{% extends 'layout/base.html.twig' %}

{% block content %}
<div class="intro-y box p-5 mt-5">
    <p class="text-slate-500">Endpoint AI non disponibile come pagina standalone.</p>
</div>
{% endblock %}
