{# Standalone HTML fragment for AJAX injection into the er-modal "picked" panel.
   Receives: fatture (grouped), total (float), label (string), month (YYYY-MM). #}
{% if fatture is empty %}
    <p class="er-empty">Nessuna fattura emessa nel periodo.</p>
{% else %}
        <div style="display:flex; justify-content:flex-end; margin-bottom:10px;">
            <a href="/billing/clients/emesse-month/export?month={{ month }}"
               style="display:inline-flex; align-items:center; gap:6px; padding:8px 16px; border-radius:9px;
                      background:linear-gradient(135deg,#16a34a,#15803d); color:#fff;
                      font-size:13px; font-weight:600; text-decoration:none;">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
                    <polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/>
                </svg>
                Scarica Excel
            </a>
        </div>
        <div style="overflow-x:auto;">
            <table class="er-table er-fatture-table">
                <thead>
                    <tr>
                        <th style="width:32px;"></th>
                        <th>Data</th>
                        <th>Numero</th>
                        <th>Cliente</th>
                        <th class="center">Voci</th>
                        <th class="right">Imponibile</th>
                    </tr>
                </thead>
                <tbody>
                    {% for f in fatture %}
                        <tr class="er-fattura-row" data-er-toggle="picked-{{ loop.index }}">
                            <td class="er-toggle-cell">
                                <span class="er-chevron">▸</span>
                            </td>
                            <td style="white-space:nowrap;">{{ f.data ? f.data|date('d/m/Y') : '—' }}</td>
                            <td style="white-space:nowrap; font-weight:600;">{{ f.numero_label }}</td>
                            <td>
                                {% if f.clienti is empty %}
                                    —
                                {% else %}
                                    {{ f.clienti|join(', ') }}
                                {% endif %}
                            </td>
                            <td class="center">{{ f.rows|length }}</td>
                            <td class="right" style="font-weight:600;">€ {{ f.totale|number_format(2, ',', '.') }}</td>
                        </tr>
                        <tr class="er-fattura-detail" data-er-detail="picked-{{ loop.index }}" style="display:none;">
                            <td></td>
                            <td colspan="5" style="padding:0;">
                                <table class="er-detail-table">
                                    <thead>
                                        <tr>
                                            <th>Cantiere</th>
                                            <th>Descrizione</th>
                                            <th class="right">Imponibile</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        {% for r in f.rows %}
                                        <tr>
                                            <td>{{ r.nome_cantiere ?: '—' }}</td>
                                            <td style="max-width:520px;">{{ r.descrizione ?: '—' }}</td>
                                            <td class="right">€ {{ (r.totale_imponibile ?? 0)|number_format(2, ',', '.') }}</td>
                                        </tr>
                                        {% endfor %}
                                    </tbody>
                                </table>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
                <tfoot>
                    <tr>
                        <td colspan="5" class="right" style="font-weight:600;">Totale</td>
                        <td class="right" style="font-weight:600;">€ {{ total|number_format(2, ',', '.') }}</td>
                    </tr>
            </tfoot>
        </table>
    </div>
{% endif %}
